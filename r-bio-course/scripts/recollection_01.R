install.packages("pacman")
library(pacman) # not sure whether quatation marks are required or not
p_load("readxl")





setwd("/home/motall/r_cabinet/r-bio-course/scripts/")

proj <- normalizePath("..", mustWork = FALSE) # go one folder "up" from scripts/ (adjust if needed)
data_dir <- file.path(proj, "data") # join folder names the OS-correct way
fig_dir <- file.path(proj, "figures") # where we’ll save plots later

# Check they exist — returns TRUE/FALSE (good quick sanity check)
dir.exists(data_dir)
dir.exists(fig_dir)








proj <- normalizePath("..", mustWork = FALSE)
data_dir <- file.path(proj, "data")
fig_dir <- file.path(proj, "figures")

read_tab <- function(fname) {

  read.delim(file.path(data_dir, fname),
   header = TRUE, sep = "\t",
    #other arguments
    na.strings = c("", "NA")
  )
}





read_tab <- function(fname) {
  read.delim(file.path(data_dir, fname),
    header = TRUE, sep = "\t",
    # ... other arguments
    na.strings = c("", "NA")
  )
}



fish_data <- read_tab("fish.txt")


head(fish_data)
View(fish_data)
summary(fish_data)

median(fish_data$length)
mean(fish_data$length)



var(fish_data$length)
sd(fish_data$length)

hist(fish_data$length, breaks=50, main=" Fishy Business", xlab="Length (cm)", col="lightblue")

subset(fish_data, length > 6 & genotype != "A")

boxplot(length ~ genotype, fish_data, main="Length of Fish with Respect to Genotype", xlab="Genotype", ylab="Length (cm)", col=c("lightblue", "red"))


















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































eg <- read_tab("egypt_skulls.txt")


reg_model_ybp_bl <- lm(BL ~ ybp, data = eg)
summary(reg_model_ybp_bl)




model <- lm(BL ~ ybp, data = eg)
t_value <- summary(model)$coefficients["ybp", "t value"]
t_value
